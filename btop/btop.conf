# Minimal btop preferences.
# Keep this file sparse so older btop versions can use their own defaults.

color_theme = "TTY"
vim_keys = True
update_ms = 500
proc_sorting = "memory"
use_fstab = False
io_mode = True
